# Keuangan PWA
Aplikasi keuangan pribadi React + Vite + Google Sheets.

## Menjalankan
npm install
npm run dev

## Production
npm run build

Output: dist

Deploy ke Vercel/Netlify dengan build command `npm run build` dan publish directory `dist`.
PWA harus dibuka melalui HTTPS agar dapat di-install di HP.
